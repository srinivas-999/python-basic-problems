# 1.write a python programming that takes a user input and prints it ?
user_input=input ("stay consistent")
print(f"you entered:{user_input}")


# 2. write a python programming to check if number is +ve ,-ve or zero
num=float(input("enter a number"))
if num>0:
    print("it is positive")
elif num<0:
    print("it is nagative")
else :
    print("it is zero")
    
# 3. write a python programming to find largest of three numbers
num1 = float(input("Enter first number: "))
num2 = float(input("Enter second number: "))
num3 = float(input("Enter third number: "))

if (num1 >= num2) and (num1 >= num3):
    largest = num1
elif (num2 >= num1) and (num2 >= num3):
    largest = num2
else:
    largest = num3

print(f"The largest number is {largest}")
           
# 4. write a python programming to calculate the factorial of a number


def factorial(n):
    if n==0:
        return 1
    else:
        return n* factorial(n-1)
num=int(input("enter a number:"))
print(f" the factorial of {num} is {factorial(num)}")

# 5.create a variables of different data types : integer ,float,string,and boolean and print their values and types
integr_var=10
float_var=20.3
string_var="Srinivas"
boolean_var=True
print(f"integer value {integr_var},{type(integr_var)}")
print(f"float value {float_var},{type(float_var)}")
print(f"string value {string_var},{type(string_var)}")
print(f"boolean value {boolean_var},{type(boolean_var)}")

# 6. write a python programming to swap the values of two variables
a,b=10,20         #  a=10 b= 20
a,b=b,a            # a=b  b=a swapping
print(f"a={a},b={b}")

#          for easy to understand
a = 5
b = 10
print(f"Before swap: a = {a}, b = {b}")

# Swapping
a, b = b, a
print(f"After swap: a = {a}, b = {b}")

# 7.write a python program to convert celsius to farenheit
celsius = float(input("Enter temperature in Celsius: "))
fahrenheit = (celsius * 9/5) + 32
print(f"{celsius}°C is equal to {fahrenheit}°F")

# 8.write a program to concatenate of two strings
str_1="adavani "
str_2="srinivasulu"
concatenate_strings=str_1+ " "+ str_2
print(concatenate_strings)

# 9.write a program to check if a variable is of a specific data data type
var = 10.5
if isinstance(var, float):
    print(f"{var} is a float")
else:
    print(f"{var} is not a float")

# 10.write a python program to perform arithmatic operators :addition ,sub,mul,div
a = 5
b = 3

print(f"Addition: {a} + {b} = {a + b}")
print(f"Subtraction: {a} - {b} = {a - b}")
print(f"Multiplication: {a} * {b} = {a * b}")
print(f"Division: {a} / {b} = {a / b}") 
